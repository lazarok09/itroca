"use client";

import * as Styled from "./styles";

export const Footer = () => {
  return (
    <Styled.FooterWrapper>
      iTroca &reg; - Todos os direitos reservados
    </Styled.FooterWrapper>
  );
};
